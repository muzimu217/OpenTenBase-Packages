#!/bin/sh
set -e
case "$1" in
    remove|upgrade|deconfigure)
        if [ -x /usr/bin/opentenbase-ctl ]; then
            /usr/bin/opentenbase-ctl stop >/dev/null 2>&1 || true
        fi
        ;;
esac

#DEBHELPER#

exit 0
